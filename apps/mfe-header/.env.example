# DSN for the mfe-header Sentry project.
# Used at BUILD TIME by @sentry/vite-plugin to inject moduleMetadata into
# the bundle. The shell reads this metadata off stack frames to route
# errors to this project via makeMultiplexedTransport.
SENTRY_DSN=https://your-key@oXXXXXX.ingest.sentry.io/XXXXXXX
